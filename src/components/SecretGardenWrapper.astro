---
// 导入React组件
import SecretGarden from './react/SecretGarden'

// 获取传入的props
const { title, items, backgroundImage } = Astro.props
---

<!-- client:visible 指令确保组件在可见时激活 -->
<SecretGarden title={title} items={items} backgroundImage={backgroundImage} client:visible />

