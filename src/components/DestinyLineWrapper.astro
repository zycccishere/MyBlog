---
// 导入React组件
import DestinyLine from './react/DestinyLine'

// 获取传入的props
const { title, events } = Astro.props
---

<!-- client:visible 指令确保组件在可见时激活 -->
<DestinyLine title={title} events={events} client:visible />

