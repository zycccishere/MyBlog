---
// 导入React组件
import DriftBottle from './react/DriftBottle'

// 获取传入的props
const { title, defaultMessages, maxMessages } = Astro.props
---

<!-- client:visible 指令确保组件在可见时激活 -->
<DriftBottle
	title={title}
	defaultMessages={defaultMessages}
	maxMessages={maxMessages}
	client:visible
/>

