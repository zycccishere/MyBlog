---
// 导入React组件
import MemoryAlbum from './react/MemoryAlbum'

// 获取传入的props
const { title, photos } = Astro.props
---

<!-- client:visible 指令确保组件在可见时激活 -->
<MemoryAlbum title={title} photos={photos} client:visible />

